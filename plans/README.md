# Implementation Plans

Generated by the `improve` skill on 2026-07-01, against commit `2f46689`.
Execute in the order below unless dependencies say otherwise. Each executor:
read the plan fully before starting, honor its STOP conditions, and update your
row when done.

**How this set was chosen**: the audit surfaced ~30 findings across nine
categories. The maintainer was not available to select interactively, so these
7 plans were written for the **highest-leverage findings by default**
(high-confidence, clean verification story, security/correctness weighted up).
The findings that were deliberately *not* turned into plans are listed under
"Deferred" and "Considered and rejected" below so they aren't re-audited next
run — turning any of them into a plan is a one-line ask.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Verification baseline (typecheck + vitest + pure-function tests) | P1 | M | — | TODO |
| 002 | Fix store logo/banner uploads (allow replace; stop orphan rows) | P1 | S–M | — | TODO |
| 003 | Dashboard store-settings ownership check (fix IDOR) | P1 | S | — | TODO |
| 004 | Harden MercadoPago webhook (fail closed; consistent data.id; replay) | P1 | S–M | — | TODO |
| 005 | Fix product-price display (ARS, no fake decimals) | P2 | S | — | TODO |
| 006 | Invoke `deleteProduct` safely (pending + error handling) | P2 | S | — | TODO |
| 007 | Upgrade Clerk + Next to patched security releases | P1 | M | 001 | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (one-line reason) |
REJECTED (one-line rationale).

## Recommended sequence

1. **001 first** — establishes `pnpm typecheck` and `pnpm test`. Every later
   plan's verification section assumes these exist; 007 in particular should not
   be attempted without a test safety net.
2. **002–006** — independent bug/security fixes, each small and self-contained.
   Order among them is flexible; 002 (broken feature) and 003 (IDOR) are the
   highest user/security value, 004 is the money-path hardening.
3. **007 last** — the framework/auth upgrade is the riskiest change; run it once
   the test baseline (001) is green so regressions are catchable.

## Dependency notes

- 007 depends on 001: bumping Clerk (6.9→6.39) and Next (16.0→16.2+) with no
  tests is risky; land the verification baseline first.
- 002–006 each *reference* plan 001's `pnpm test` for optional regression tests
  but can be executed and verified via typecheck/build/manual checks even if 001
  hasn't landed. Each notes this in its Test plan.
- No plan depends on another among 002–006; they touch disjoint files.

## Deferred findings (real, vetted — not yet turned into plans)

These survived vetting but were left out of the default set to keep it focused.
Each is a candidate for a future plan:

**Subscription/billing correctness (MED confidence, money path — do after 001's
integration harness):**
- Stale preapproval webhook can clobber a newer subscription — `api/mercadopago/route.ts:28-45`
  (`external_reference` fallback overwrites the current sub on out-of-order delivery).
- Eager billing-page refresh can regress ACTIVE → PENDING — `queries/subscription.ts:44-52`.
- `/api/checkout` doesn't block re-subscribing while ACTIVE → duplicate MP
  preapproval / double charge — `checkout/route.ts:67-127`.
- Plan-limit enforcement is check-then-act (no transaction) — `limits.ts:15-53`.

**Correctness (MED):**
- `Store.name` globally unique but create checks per-user; blocks legitimate
  multi-tenant signups — `schema.prisma:58`, `store.tsx:173-178` (needs a migration).
- Product `price` validated as `z.string()` then `Number()` into an `Int` column;
  decimals/negatives/NaN slip through — `validations/product.ts:8,26`.

**Security (lower reachability):**
- Menu-import action lacks server-side file type/size validation — `import-menu.ts:19-54`.
- No response-hardening headers (CSP/X-Frame-Options/etc.) — `next.config.js`.
- Raw internal error text returned to clients — `store.tsx:286-294`, `import-menu.ts:118-123`.
- `Payment.rawPayload` stores the full MP payload incl. payer PII — `api/mercadopago/route.ts:62-80`.
- Exported `getProduct` server action does no auth (public-data enumeration) — `product.tsx:17-19`.

**Perf:**
- Sitemap runs two uncached full-table scans per request — `sitemap.ts:7-29`.
- Client-side react-query fetch for server-available data (pulls whole
  react-query runtime for one consumer) — `use-products.ts`, `edit-product.tsx`.
- Dead Suspense boundary wrapping already-resolved data — `dashboard/stores/page.tsx`.

**Tech debt / deps / docs:**
- `.tsx` server-action files with no JSX (`store.tsx`, `product.tsx`) → rename to `.ts`.
- Ownership check re-implemented in 4 sites → extract a shared helper.
- Three divergent server-action error-contract shapes → converge.
- Soft-delete half-implemented (`deletedAt` columns/filters exist; deletes are
  hard) → decide soft vs. drop the column + filters + redundant `deleteMany`.
- `unstable_noStore` used under `cacheComponents` (deprecated no-op) — `product.tsx:5,28,83`.
- Dead commented image-block + no-op `if/else` — `product-list.tsx:32-40,57-73`, `import-menu.ts:50-54`.
- `SiteConfig` type misfiled under `validations/types/`.
- TypeScript 5.3.3 / `@types/node` 20.10.4 ~2 years stale for Next 16 / React 19.
- `uploadthing` 6.x is a major behind (v7 has breaking changes) — verify before bumping.
- README roadmap wrong: shipped billing listed as "planned"; Node-18 prereq vs
  `.nvmrc` 20.10.0; getting-started env list omits Mapbox/MP vars.
- No ADR for the billing/entitlement state machine (highest-churn subsystem).
- No CI (`.github/`) running lint/typecheck/build/test.

**Direction (options for the maintainer, not defects):**
- Online ordering + checkout (headline unbuilt roadmap item; current workaround
  is a static `wa.me` link in `reserve-button.tsx`).
- Resolve the Enterprise-tier asymmetry (enum + card exist, no payable path;
  `getPlan` silently falls back to BASIC).
- Analytics dashboard over already-persisted `Payment`/`Subscription` data.
- Public read-only menu API / embeddable widget (public cached read path already exists).

## Considered and rejected (do not re-audit)

- **Missing `deletedAt` filter on the `products` include in `getStoreById`** —
  inert. Nothing in the codebase ever soft-deletes (stores/products are
  hard-deleted), so every `deletedAt: null` filter is always-true and the
  missing one is a no-op. This is latent tech-debt folded into the "soft-delete
  half-implemented" deferred item, not a live bug.
- **Public store page serving INACTIVE stores by direct link** — reads as
  by-design (INACTIVE hides from city listings but stays reachable by direct
  URL). Flag only if the maintainer says otherwise.
- **Unused dependencies** — checked; all recon-flagged deps (`image-conversion`,
  `cmdk`, `qrcode.react`, `@ai-sdk/openai`, every `@radix-ui/*`,
  `config/products.ts`) are imported and reachable. No prune to do.
- **`.env.example` vs `src/env.ts` mismatch** — none; `.env.example` is complete
  and consistent. The only stale copy is the README's inline env list (folded
  into the README deferred item).
- **MP webhook HMAC not constant-time** — false alarm; it already uses
  `crypto.timingSafeEqual`. The real webhook weaknesses (fail-open, id source,
  replay) are covered by plan 004.
